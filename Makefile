zengon: main.cpp
    g++ -o build/zengon main.cpp -lSDL2

clear:
    rm -rf build/*

install:
    # Add any installation steps here, e.g., copying the executable to a specific directory
    # Example: cp build/zengon /usr/local/bin/

